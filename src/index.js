// You can write your own logic here to determine the actual url
window.remoteapp = "http://localhost:3001"

// Use dynamic import here to allow webpack to interface with module federation code
import("./bootstrap");
